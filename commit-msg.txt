feat: stacked layout, centered dialog, department pills, and polish

- Revert sidebar split to vertical stacked layout: pure black hero
  section on top, job list with ambient glow below
- Replace Shadcn Sheet with centered Dialog (base-ui primitives) —
  bg-black/80 backdrop-blur-sm, max-w-2xl, max-h-[88vh] scrollable body
- Department filter: remove Select dropdown, expose pills with white
  active state and shadow-[0_0_15px_rgba(255,255,255,0.15)] glow
- FilterBar redesigned as horizontal centered row with wrapping on mobile
- Hero badge is now dynamic — reflects live filtered job count
- JobCard: "New" sky-blue badge on listings posted within 3 days
- Toast fix: hardcode white bg / zinc-950 text for guaranteed contrast
- Card elevation: bg-white/[0.04] border-white/[0.08], brighter hover
- Fail-safe ambient glow: fixed absolute div instead of CSS gradient
- Sidebar filter labels: text-white/50, more prominent inputs
- Marketing badge: fuchsia (was emerald — clashed with Applied state)
- Install Shadcn Dialog component (components/ui/dialog.tsx)

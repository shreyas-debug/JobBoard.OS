ux: flat technical aesthetic, Clearbit logos, and prominent Apply CTA

Logos:
- Added Clearbit Logo API URLs to all 12 companies in jobs.json
- next.config.ts: added logo.clearbit.com to allowed image domains
- JobCard renders real company logos via next/image with LetterAvatar fallback
- LetterAvatar: removed gradient fills, replaced with flat tinted palette
  (solid bg with matching text/border, no gradient-to-* classes)

Flat aesthetic (zero gradients, zero blur):
- Removed all bg-gradient-to-*, from-*, to-* across every component
- Removed all backdrop-blur-*, blur-* effects
- Hero: plain white bg, solid border-b, black heading with tracking-tight
  Status badge: flat gray-50 with solid emerald dot, no animations
- Cards: white bg inside a shared rounded-xl border container, divider lines
  between rows (list-style like Linear Jobs / Greenhouse)
  Hover: bg-gray-50 tint only — no shadow, no border shift
- FilterBar: bg-white, solid border-b, no backdrop-blur
  Active type pill: bg-gray-900 text-white (solid black)
- Badges: flat tinted backgrounds with matching border (no opacity tricks)

Apply Now CTA:
- Prominent solid bg-gray-900 button in the sheet header (always visible)
- Sticky footer bar with "Apply for this Position" + "Back to listings"
- Both CTAs use the same black solid style, no gradients
- Sheet body scrolls independently between fixed header and sticky footer

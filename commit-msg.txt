feat: apply flow with localStorage persistence, toast, and applied filter

Apply Now button (JobDetailSheet):
- Simulates 1-second network request with Loader2 spinner and "Submitting…" text
- On success: fires Sonner toast ("Application submitted!" with job title/company)
- Disabled "Applied" state (emerald chip + CheckCircle2) when job_id is in storage
- Two CTAs: header button (always in view) and sticky footer button

localStorage persistence (hooks/useAppliedJobs.ts):
- New hook reads/writes a Set of applied job IDs to localStorage key "jobboard_applied_ids"
- Survives page refresh; handles JSON parse errors and storage write failures gracefully
- Exposes: appliedIds (Set), applyToJob(id), isApplied(id)

Applied indicator on JobCard:
- Small "Applied" badge (emerald, with CheckCircle2) inline with job title
- CheckCircle2 overlay dot on the company logo avatar

"Show Applied Jobs" toggle (FilterBar):
- Custom WAI-ARIA role="switch" toggle in a secondary filter row
- Displays applied count badge next to the label
- Active state: solid black track; inactive: white with gray border
- "Clear" button and handleReset() both reset the toggle

Shadcn Sonner integration:
- Added sonner component via shadcn CLI
- Toaster mounted in layout.tsx with position="bottom-right" richColors
- ThemeProvider (next-themes) wraps app with forcedTheme="light"
